import { marketingMasterClassData } from '@/lib/data/marketingMasterClass';
import ProjectPage from './ProjectPage';

const MarketingMasterClass = () => <ProjectPage project={marketingMasterClassData} />;

export default MarketingMasterClass;
